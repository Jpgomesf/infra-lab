# The portable identity contract: the app knows only this KSA.
# Cloud overlays annotate it (GKE: iam.gke.io/gcp-service-account) to bind it
# to the identity created by infra/modules/iam.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: api
